.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

# Devnet placeholder bucket-owner keypairs (see scripts/genesis.ts) — these
# are NOT production custody, but secret keys never belong in git regardless.
scripts/.bucket-keys/
