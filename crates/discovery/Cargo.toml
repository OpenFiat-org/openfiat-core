[package]
name = "openfiat-discovery"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Peer discovery: bootstrap, routing table, peer exchange."
repository = "https://github.com/OpenFiat-org/openfiat-core"

[dependencies]
openfiat-network = { path = "../network" }
openfiat-types = { path = "../types" }
