[package]
name = "openfiat-oracles"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "External data oracle plugin architecture."
repository = "https://github.com/OpenFiat-org/openfiat-core"

[dependencies]
openfiat-types = { path = "../types" }
