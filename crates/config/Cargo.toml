[package]
name = "openfiat-config"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Configuration loading, validation, and layering (file/env/flags)."
repository = "https://github.com/OpenFiat-org/openfiat-core"

[dependencies]
openfiat-types = { path = "../types" }
