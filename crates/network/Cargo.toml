[package]
name = "openfiat-network"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Transport layer: TCP/QUIC, TLS abstraction, connection management."
repository = "https://github.com/OpenFiat-org/openfiat-core"

[dependencies]
openfiat-types = { path = "../types" }
openfiat-serialization = { path = "../serialization" }
openfiat-crypto = { path = "../crypto" }
